{
  "name": "simple",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "nx run-many --target=dev --all --output-style=stream",
    "dev:nuked": "nx run-many --target=dev:nuked --all --output-style=stream",
    "db:studio": "nx run-many --target=db:studio --all --output-style=stream"
  },
  "private": true,
  "dependencies": { "better-sqlite3": "^11.1.1", "zod": "^3.23.8" },
  "devDependencies": {
    "@nx/js": "19.3.2",
    "nx": "19.3.2"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
